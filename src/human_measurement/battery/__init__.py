"""Paper-reproducibility shim for ``human_measurement.battery``.

The research package implements Milestone 7 battery-structure discovery,
which is closed (`experimental_out_of_support`) and outside the METER
foundational paper. The supplied-structure model imports exactly one function
from it (`battery.model.spectral_item_features`); this shim provides that
function verbatim and nothing else.
"""
