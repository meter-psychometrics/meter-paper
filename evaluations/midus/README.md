# MIDUS MIDI Big Five — Table 1 row 5 (+ Extended Data development cohort)

**Paper result:** MIDUS Refresher (independent cohort, n = 2,595): factor-wise
METER vs confirmatory MIRT **0.9241-0.9747** — all prospectively frozen SCORE
gates passed; BOTH factor-correlation gates FAILED (mean 0.1333 vs 0.12, max
0.2271 vs 0.20), so real-data Phi recovery is not supported, and the paper
reports that failure. MIDUS 1 (development role, n = 6,288): 0.9048-0.9807.
Reported numbers: `derived_results/midus/` (including the frozen gate file
`d9b_replication_gates_frozen.json`).

**Source data:** MIDUS 1 (ICPSR 2760) and MIDUS Refresher (ICPSR 36532) via
ICPSR/NACDA. ICPSR's AI policy prohibits providing these data to
data-retaining public AI services and requires permission for qualifying AI
use; the frozen analysis was strictly local, and any reproduction must
document the terms in force for the downloaded files. Never redistribute
participant-level data.

**Expected input:** the 25 MIDI Big Five adjective items in the FROZEN column
order recorded in `d9a_charter_frozen.json` (neuroticism 4, extraversion 5,
openness 7, conscientiousness 4, agreeableness 5 — exact variable names in
the charter's item_mapping block), 4-point codes 0-3 after the charter's
reverse-keying; missing = empty. `factor_map.json` and `factor_labels.json`
carry the frozen assignment in that column order.

**Frozen procedure:** research scripts `d9a_transfer_run.py`
(9471e0cc3eafa1ce…) and `d9b_replication_run.py` (3eb286056e39f6ce…),
reviewer-only. Comparator: confirmatory MIRT with the supplied structure —
the exact `mirt.model` string is generated by
`human_measurement.mapped.mapping.mirt_model_string` (shipped).

**Reproduce:**

```
python run_meter.py --responses responses.csv \
    --comparator mirt_factor_scores.csv --output result.json
```

Comparisons are factor-wise, signed and unpermuted, exactly as in the paper.
